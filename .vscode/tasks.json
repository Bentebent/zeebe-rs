// tasks.json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build current example",
            "type": "cargo",
            "command": "build",
            "args": [
                "--example=${fileBasenameNoExtension}",
            ],
        }
    ]
}